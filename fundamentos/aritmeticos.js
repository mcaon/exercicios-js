const [a, b, c, d] = [3, 5, 1, 15];

const soma = a + b + c + d;

const subtracao = d - b;

const multiplicacao = a * b;

const divisao = d / a;

const modulo = a % 2; // RESTO DA DIVISAO de a / 2

console.log(soma, subtracao, multiplicacao, divisao, modulo);


// a + b // Operador IN-FIX
// ++b // Operador PRE-FIX
// b++ // Operador POST-FIX