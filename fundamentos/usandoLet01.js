var numero = 1;
{
    let numero = 2;
    console.log('dentro', numero);
}
console.log('fora', numero);

    // variavel declarada como let tem escopo global, escopo de funcao e escopo de bloco
// enquanto variavel declarada com var possui apenas escopo global e escopo de funcao